# Known noise keywords — element names containing these are skipped during auto-detection.
# This list is intentionally simple and will miss site-specific noise.
# Will be replaced in a future version.

NOISE_KEYWORDS = [
    'dismiss', 'close', 'dialog', 'reload', 'error', 
    'flash', 'requiredfield', 'locale', 'resetpassword'
]